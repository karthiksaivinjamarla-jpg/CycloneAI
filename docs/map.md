# Interactive map

The dashboard map uses Leaflet with OpenStreetMap tiles. The frontend draws the observed track, AI forecast track, forecast points, cyclone center, and a prototype uncertainty corridor from the typed analysis response.

The map is a visualization layer only. It does not claim operational forecast accuracy.
