# Training

Training entry points will live here.

Before training, record:
- dataset version
- storm split definition
- feature/channel configuration
- model version
- random seed
- hyperparameters
- evaluation metrics

Never report mock/demo outputs as trained-model performance.
